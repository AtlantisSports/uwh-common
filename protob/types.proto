syntax = "proto2";

import "google/protobuf/descriptor.proto";

extend google.protobuf.EnumValueOptions {
  // message can be transmitted via wire from deck-ref-box to consumers
  optional bool wire_in = 50002;

  // message can be transmitted via wire from consumers to deck-ref-box
  optional bool wire_out = 50003;
}
